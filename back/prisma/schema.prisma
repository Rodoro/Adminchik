generator client {
    provider = "prisma-client-js"
    output   = "./generated"
}

datasource db {
    provider = "postgresql"
    url      = env("POSTGRES_URI")
}

model Staff {
    id String @id @default(uuid())

    email    String @unique
    password String
}
